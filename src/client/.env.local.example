# Vite picks .env.local up automatically when present. This file is
# gitignored. Run `yarn setup:local` from the project root to populate
# it (alongside src/server/.env.local).

VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=__from_supabase_status__
